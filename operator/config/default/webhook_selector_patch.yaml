# This patch adds namespace and object selectors to the mutating webhook
# to prevent it from intercepting ALL pod CREATEs cluster-wide.
# Only pods in rossoctl-enabled namespaces with rossoctl.io/type labels are intercepted.
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: mutating-webhook-configuration
webhooks:
- name: inject.rossoctl.io
  namespaceSelector:
    matchExpressions:
    - key: kubernetes.io/metadata.name
      operator: NotIn
      values:
      - kube-system
      - kube-public
      - kube-node-lease
      - rossoctl-operator-system
    matchLabels:
      rossoctl-enabled: "true"
  objectSelector:
    matchExpressions:
    - key: rossoctl.io/type
      operator: In
      values:
      - agent
      - tool
    - key: rossoctl.io/inject
      operator: NotIn
      values:
      - disabled
  timeoutSeconds: 10
